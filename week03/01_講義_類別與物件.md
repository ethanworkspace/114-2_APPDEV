# W03 講義：類別與物件（Classes & Objects）

> **APP 開發課程** ｜ 第 3 週  
> **主題**：OOP 精神、定義 Class、建立 Object、屬性、方法、建構子  
> **銜接**：W02 流程控制與陣列 → 本週類別與物件 → W04 封裝與繼承  
> **GFG 教材**：[OOP Concepts](https://www.geeksforgeeks.org/java/object-oriented-programming-oops-concept-in-java/)、[Classes & Objects](https://www.geeksforgeeks.org/java/classes-objects-java/)

---

## 1. 什麼是物件導向程式設計（OOP）？

之前寫的程式都是「做什麼事」的流程，例如用 for 迴圈印出九九乘法表。OOP 換一個思維方式：先想「有哪些東西」，再想「這些東西能做什麼」。

生活類比：設計一個港口管理系統，你會先想到有哪些「東西」：船、碼頭、貨櫃、船員。每個東西有自己的特性和行為。這些「東西」就是物件。

### OOP 四大特性預覽

| 特性 | 一句話 | 本週學 | 後續 |
|------|-------|:-----:|------|
| 封裝 | 隱藏細節，只開放介面 | | W04 |
| 繼承 | 子類別繼承父類別 | | W04 |
| 多型 | 同名方法，不同行為 | | W05 |
| 抽象 | 定義規格不實作 | | W06 |

本週先學最基礎的「類別與物件」，這是四大特性的基石。

---

## 2. 類別（Class）與物件（Object）

**類別**是設計圖，**物件**是根據設計圖做出來的實體。

| 概念 | 類比 | Java |
|------|------|------|
| 類別 Class | 船的設計圖 | `class Ship { ... }` |
| 物件 Object | 根據設計圖造出的一艘船 | `Ship s = new Ship();` |

一份設計圖可以造出很多艘船，每艘船是獨立的物件，有自己的名字和狀態。

---

## 3. 定義類別

### 3.1 基本結構

```java
class Ship {
    // 屬性（Attributes）：這個東西「有什麼」
    String name;
    int capacity;
    double speed;

    // 方法（Methods）：這個東西「能做什麼」
    String info() {
        return name + "，載量 " + capacity + " 噸，速度 " + speed + " 節";
    }

    void sail() {
        System.out.println(name + " 正在航行");
    }
}
```

### 3.2 建立物件

用 `new` 關鍵字根據類別建立物件：

```java
Ship s1 = new Ship();       // 建立一個 Ship 物件
s1.name = "遠洋號";         // 設定屬性
s1.capacity = 5000;
s1.speed = 20.5;

Ship s2 = new Ship();       // 建立另一個 Ship 物件
s2.name = "海豐號";
s2.capacity = 3000;
s2.speed = 15.0;

System.out.println(s1.info());  // 遠洋號，載量 5000 噸，速度 20.5 節
System.out.println(s2.info());  // 海豐號，載量 3000 噸，速度 15.0 節
s1.sail();                      // 遠洋號 正在航行
```

s1 和 s2 是兩個獨立的物件，修改 s1 不會影響 s2。

---

## 4. 建構子（Constructor）

每次建立物件都要一行一行設定屬性很麻煩。建構子讓你在建立物件的同時就初始化屬性。

### 4.1 基本建構子

```java
class Ship {
    String name;
    int capacity;
    double speed;

    // 建構子：和類別同名，沒有回傳值
    Ship(String name, int capacity, double speed) {
        this.name = name;           // this 指向目前這個物件
        this.capacity = capacity;
        this.speed = speed;
    }

    String info() {
        return name + "，載量 " + capacity + " 噸，速度 " + speed + " 節";
    }
}
```

使用建構子建立物件：

```java
Ship s1 = new Ship("遠洋號", 5000, 20.5);   // 一行搞定
Ship s2 = new Ship("海豐號", 3000, 15.0);

System.out.println(s1.info());
System.out.println(s2.info());
```

### 4.2 this 關鍵字

`this` 指向「目前這個物件」。當參數名稱和屬性名稱相同時，用 `this` 區分：

```java
Ship(String name, int capacity, double speed) {
    this.name = name;         // this.name 是屬性，name 是參數
    this.capacity = capacity;
    this.speed = speed;
}
```

### 4.3 建構子多載

一個類別可以有多個建構子，參數不同：

```java
class Ship {
    String name;
    int capacity;
    double speed;

    // 建構子 1：完整參數
    Ship(String name, int capacity, double speed) {
        this.name = name;
        this.capacity = capacity;
        this.speed = speed;
    }

    // 建構子 2：只給名字，其他用預設值
    Ship(String name) {
        this.name = name;
        this.capacity = 1000;    // 預設值
        this.speed = 10.0;       // 預設值
    }

    String info() {
        return name + "，載量 " + capacity + " 噸，速度 " + speed + " 節";
    }
}
```

```java
Ship s1 = new Ship("遠洋號", 5000, 20.5);  // 用建構子 1
Ship s2 = new Ship("小漁船");               // 用建構子 2

System.out.println(s1.info());  // 遠洋號，載量 5000 噸，速度 20.5 節
System.out.println(s2.info());  // 小漁船，載量 1000 噸，速度 10.0 節
```

---

## 5. 方法（Methods）

方法定義物件「能做什麼」。

### 5.1 有回傳值的方法

```java
class Rectangle {
    double width;
    double height;

    Rectangle(double width, double height) {
        this.width = width;
        this.height = height;
    }

    // 回傳 double
    double area() {
        return width * height;
    }

    // 回傳 String
    String describe() {
        return "長方形 " + width + " x " + height + "，面積 = " + area();
    }
}
```

### 5.2 有參數的方法

```java
class Ship {
    String name;
    double fuelLevel;

    Ship(String name) {
        this.name = name;
        this.fuelLevel = 100.0;
    }

    // 有參數、沒回傳值
    void sail(int km) {
        double fuelUsed = km * 0.5;
        fuelLevel -= fuelUsed;
        System.out.println(name + " 航行 " + km + " 公里，剩餘油量：" + fuelLevel);
    }

    // 有參數、有回傳值
    double estimateFuel(int km) {
        return km * 0.5;
    }
}
```

```java
Ship s = new Ship("遠洋號");
s.sail(50);                                    // 遠洋號 航行 50 公里，剩餘油量：75.0
System.out.println("預估油耗：" + s.estimateFuel(100));  // 預估油耗：50.0
```

---

## 6. 完整範例：海洋生物

把以上觀念整合，建立一個海洋生物的類別：

```java
class MarineLife {
    String name;
    String species;
    String habitat;
    double weight;

    // 建構子
    MarineLife(String name, String species, String habitat, double weight) {
        this.name = name;
        this.species = species;
        this.habitat = habitat;
        this.weight = weight;
    }

    // 簡單建構子
    MarineLife(String name, String species) {
        this.name = name;
        this.species = species;
        this.habitat = "未知";
        this.weight = 0;
    }

    // 方法
    String info() {
        return name + "（" + species + "），棲息地：" + habitat + "，體重：" + weight + " kg";
    }

    String move() {
        return name + " 在水中移動";
    }

    String eat(String food) {
        return name + " 正在吃 " + food;
    }
}

public class MarineDemo {
    public static void main(String[] args) {
        MarineLife nemo = new MarineLife("尼莫", "小丑魚", "珊瑚礁", 0.1);
        MarineLife whale = new MarineLife("藍藍", "藍鯨", "太平洋", 150000);
        MarineLife unknown = new MarineLife("神秘生物", "未知種");

        System.out.println(nemo.info());
        System.out.println(nemo.move());
        System.out.println(nemo.eat("海葵"));
        System.out.println();
        System.out.println(whale.info());
        System.out.println(whale.move());
        System.out.println(whale.eat("磷蝦"));
        System.out.println();
        System.out.println(unknown.info());
    }
}
```

輸出：
```
尼莫（小丑魚），棲息地：珊瑚礁，體重：0.1 kg
尼莫 在水中移動
尼莫 正在吃 海葵

藍藍（藍鯨），棲息地：太平洋，體重：150000.0 kg
藍藍 在水中移動
藍藍 正在吃 磷蝦

神秘生物（未知種），棲息地：未知，體重：0.0 kg
```

---

## 7. 常見錯誤

| 錯誤 | 原因 | 修正 |
|------|------|------|
| `NullPointerException` | 物件沒有用 new 建立就使用 | 確保 `new` 之後才呼叫方法 |
| 建構子有 `void` | 建構子不需要寫回傳型別 | 拿掉 `void` |
| `this` 忘記寫 | 參數名和屬性名一樣時沒用 this 區分 | 加上 `this.` |
| 一個檔案多個 public class | 只有和檔名同名的 class 可以加 public | 其他 class 拿掉 public |

---

## 8. 銜接說明

| 方向 | 連結 |
|------|------|
| ← W02 | 本週的類別方法用到 W02 的流程控制和字串處理 |
| → W04 | 下週會把屬性改成 private 實現封裝，並用 extends 實現繼承 |
| → W05 | W05 多型會讓不同子類別的同名方法有不同行為 |
| → 專題 | MarineLife 類別會在後續的 APP 專題中持續擴展 |

---

## GFG 參考資源

| 主題 | 連結 |
|------|------|
| OOP 觀念總覽 | [OOP Concepts](https://www.geeksforgeeks.org/java/object-oriented-programming-oops-concept-in-java/) |
| 類別與物件 | [Classes and Objects](https://www.geeksforgeeks.org/java/classes-objects-java/) |
| 建構子 | [Constructors in Java](https://www.geeksforgeeks.org/java/constructors-in-java/) |
| this 關鍵字 | [this Keyword](https://www.geeksforgeeks.org/java/this-reference-in-java/) |
| 方法 | [Methods in Java](https://www.geeksforgeeks.org/java/methods-in-java/) |
